import 'package:flutter/material.dart';

class GachaPuzzlePage extends StatelessWidget {
  const GachaPuzzlePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gacha Puzzle')),
      body: const Center(child: Text('Gacha Puzzle - placeholder')),
    );
  }
}
